provider: platform

workspace:
  - './*'

defaults:
  provider: terraform

output:
  postgres_host: './postgres:database_host'
  app_url: './express-service:app_url'
# TODO: explicit providers, default providers are implicit @infra-glue/provider-$provider
# providers_map:
#   whataber: '@3rd-party/provider-whatever'
