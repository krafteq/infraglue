provider: platform

workspace:
  - './*'

output:
  postgres_host: './postgres:database_host'
  app_url: './express-service:app_url'
  network_name: './docker-network:network_name'
  redis_connection_string: './redis:redis_connection_string'
# TODO: explicit providers
# providers_map:
#   whataber: '@3rd-party/provider-whatever'

# We can specify defaults which can be overridden in each workspace.
# In the case of a mix of pulumi and terraform I don't see how defaults could be useful at all.
#envs:
#  qa:
#    backend_config:
#    var_files:
#    vars:
#  dev:
#    backend_config:
#    var_files:
#    vars:
